class IndustryScore {
  String title;
  String companyName;
  DateTime startDate;
  DateTime endDate;
  final double? score;

  IndustryScore({
    required this.title,
    required this.companyName,
    required this.startDate,
    required this.endDate,
    required this.score,
  });
}
